extends layouts
include partials/watchnow/movie-card
block content
    h1 Torneo
    p sessione attuale: #{session_id}
    include partials/watchnow/exit
    .container.main-content
        .row
            .col
                +movie-card(movie, config)(class="left-movie-card")
            .col
                +movie-card(movie, config)(class="right-movie-card")
        .row
            .col
                include partials/watchnow/panel-tournament
block scripts
    script(src="/js/watchnow-tournament.js")
    script.
        let prefix_poster_path = "#{config.images.base_url}" + "#{config.images.backdrop_sizes[0]}";

